import React from 'react';

const Catalogo: React.FC = () => {
    return (
        <div>
            <h1>Catálogos de Plantas</h1>
            {/* Conteúdo do catálogo */}
        </div>
    );
};

export default Catalogo;
